# HR V6 — Post-Ship Fixes (session state)

**Branch:** `feature/hr-v6-postship-fixes` (off main `4d8031b`). **NOT pushed** — awaiting Adeel review. **Additive only; no forbidden-file edits.**
**Date:** 2026-05-30. Full diagnosis + architecture map: [`docs/.hr-v6-diagnosis.md`](.hr-v6-diagnosis.md).

## Headline
Most of Adeel's reported "bugs" were **pure frontend wiring gaps over backends that already exist + work**. Landed the clear Tier-A fixes; scoped the large builds as follow-ups.

## Fixes landed (committed, verified tsc=0 + Playwright 3/3 against local preview)
| Commit | Fix | What it does |
|---|---|---|
| `b021d69` | **Employee Edit + Terminate** | Wired the dead Edit menu item → prefilled Edit dialog (`updateEmployee`); added admin-gated **Terminate** (soft-delete) dialog with required reason (`terminateEmployee`). Backend (n8n HR7/HR8), DB cols (`employment_status`/`termination_reason`/`termination_date`), audit (`hr_audit_logs`), and both hook mutations already existed + were unused. `useIsHRAdmin()` gates Terminate. |
| `42e50b2` | **Training AI course + video** | Added `generateCourse` (`/hr/training/generate`) + `generateAvatarVideo` (`/hr/training/generate-avatar-video`) mutations + a "Generate AI Course" dialog + per-card "Generate Avatar Video" button. The Claude/Gemini generator + HeyGen pipeline were built+proven but had **0 UI call-sites** (the 1 existing video came from a manual curl). |
| `41b0ef3` | **Performance 360° feedback + Goals date** | Wired the dead "Start/Request Feedback" buttons → functional dialog + new `createFeedback` mutation (stores as `hr_performance_reviews` `review_type='360'`; CHECK verified to allow it). Fixed Goals: UI read `target_date` but column is `due_date` → date badge never showed + Add-Goal date silently dropped. |
| `7882e54` | **Onboarding modal persistence** | `OnboardingFlow.tsx` only (NOT `Layout.tsx`): dismissal now persists on **every** close path (X/Esc/outside-click), not just Skip/Get-Started, so it stops reappearing on reload. (It does NOT re-open every navigation — that was a Playwright fresh-context artifact.) |

**Verification:** `tests/hr-v6-verify.spec.ts` (project `hr-v6-verify`) against local preview build, with a modal-dismiss helper → **3/3 PASS**: Employee Edit+Terminate dialogs open (prefilled + reason field), Give/Request Feedback dialogs open, Generate-AI-Course dialog opens. Screenshots: `tests/screenshots/v6/`. **Non-destructive** — dialogs opened, no destructive saves executed on prod data (backends independently confirmed active).

## Architecture map (Adeel's questions, answered — see diagnosis doc for full detail)
- **AI interviews logged:** `hr_ai_interviews` (status, `ai_score`, `recommendation`, `actual_duration_seconds`). **Transcript:** `.transcript` (jsonb). **Audio:** `.recording_url` = VAPI's hosted URL (no Supabase bucket).
- **Candidate outreach:** intended `hr_recruitment_outreach` — **0 rows all-time; NO workflow writes it** (outreach automation does not exist).
- **Stuck "Scheduled" interview:** Interview-Call workflow `bcaK7Lxd0HgtVfqW` has **0 executions ever** (nothing auto-dials) + candidate has no phone + cosmique has no VAPI config. The AI-interview feature has never completed end-to-end for any tenant.
- **"5 sourced, 0 actioned":** screening (Claude) + auto-pipeline DID run (1 advanced, 3 held, 1 rejected); the gaps are (a) no outreach workflow, (b) apify enrichment returns no email/phone, (c) a "hold" idempotency bug (723 dup decisions for 5 candidates).

## NOT done this session — scoped as follow-up builds (documented, not blind-built)
- **Candidate outreach automation** (LARGE new build — `hr_recruitment_outreach` is dead).
- **AI-interview end-to-end** (auto-dial trigger + candidate phone enrichment + per-tenant VAPI creds — needs-user-action + medium build).
- **Sourcing status honesty + funnel clarity** (medium — current "Found N / Matched 0 / Contacted 0" is misleading).
- **Recruitment auto-pipeline "hold" idempotency** (n8n fix — stops 723 dup decisions).
- **Performance My Reviews tab** (stub) + **Team Reviews** blank names (needs employee/cycle join).
- **Training schema promotion** (AI content lives in `provider` JSON; no `program_id` FK; 5 training types unbuilt) + HeyGen video sync-hang (~4min) → make the n8n workflow async.
- **AI-Review backend 400** (`A0M2juuizluBwASl`, OpenAI-credits bug #97).
- **Candidate row-click** (works via the row's "View Profile" dropdown today; whole-row click is a discoverability nicety).

## To ship after Adeel review
`git checkout main && git pull && git merge feature/hr-v6-postship-fixes && git push origin main` → Lovable Publish. (5 commits ahead of main.)
