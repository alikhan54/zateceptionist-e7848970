# HR Custom Lecturer Avatar — build record (2026-06-03)

**Branch:** `feature/hr-custom-avatar` (off `feature/hr-chaptered-videos` — the chaptered foundation is unshipped, so this extends that branch, not main). Tenant: Zate. **No push.**

## What was created (one-time, real)
- **Custom avatar = HeyGen "Talking Photo"** from `D:/420-system/adeel pic.jpeg` (viewed + verified: clear, front-facing, eyes visible, NO sunglasses). Upload → `talking_photo_id = f17ae359459d43809d4be7ddc7ae08be` (instant, no training wait).
- **Voice = stock** `b6b1RN71VEPtvSvZCuC1` — **"Wise Indian Sage"** (HeyGen, English, male, Indian accent — closest to Pakistani/South-Asian; clear lecturer tone). *Voice cloning has no public HeyGen API (`/v2/voice/clone`, `/v1/voice.clone.list` → 404 — dashboard-only), so a stock voice was used per plan.* Alternatives to swap: `0GiuqxlLjhkwADaSIDWH` (Hindi Vlogger 20s, younger), `evZvNNzUH9RxaEHHzerf` (Wise Hindi Grandfather, older).
- **Stored on Zate** `tenant_config.features` (additive, no migration): `heygen_talking_photo_id`, `heygen_voice_id`, `heygen_avatar_label="Adeel - Zate Lecturer"`, `heygen_avatar_preview_url`. Idempotent (skips if already present).

## Backend (n8n — live, not git)
- **Generator `cMU1weyCZFP3kVrw` patched** (backup in `.tmp_diag`): when `body.avatar_mode==='custom'` (or `talking_photo_id` passed), it reads the tenant's `features.heygen_talking_photo_id` + `heygen_voice_id` and sends `character:{type:'talking_photo', talking_photo_id}` + the custom voice; otherwise the default `Anna_public_3` + stock voice (back-compat). `callback_id=chapter_id` unchanged; receiver/poll unchanged.

## Frontend (git, this branch)
- `components/hr/CourseChapters.tsx`: **avatar picker** — a Select ("Adeel - Zate Lecturer" / "Default (Anna)") with the custom avatar's **preview thumbnail**, shown when the tenant has a custom avatar. Selected mode passed to generate.
- `hooks/useHR.ts`: `generateChapters` accepts `avatar_mode` → webhook.
- `pages/hr/Training.tsx`: player passes the picked mode.

## E2E result (REAL, Playwright vs local preview — `tests/chaptered-custom-avatar-verify.spec.ts`)
Leadership course, chapters generated with the **custom avatar**:
- **ch0 "Visionary Leadership in Technology" → `ready`**, real HeyGen `video_url`. The player `<video>` **actually loaded** it: `readyState=4`, `videoWidth=1280`, `duration=26.76s`. **Thumbnail viewed = ADEEL** (the source photo — blazer/white shirt/beard), confirming it is the custom avatar, not Anna.
- A chapter **generating** with the custom avatar (ch2) captured.
- Avatar picker shows **"Adeel - Zate Lecturer"** + preview thumbnail.
- Screenshots (`tests/screenshots/custom-avatar/`): `01_avatar_picker`, `02_chapter_generating`, `03_custom_avatar_video_loaded`. 1/1 pass. tsc clean.

## Cost incurred
1 talking-photo upload (free) + 3 short (~25–27s) chapter videos rendered (HeyGen credits) — well within the 4,693-credit quota.

## Honest caveats
- **Voice clone not done** (no public API). Stock "Wise Indian Sage" used; swap by setting `features.heygen_voice_id`.
- **n8n restart during this build**: the chapter-generator patch was blocked by n8n's TypeORM pool re-sticking (`503 "Database is not ready"`, likely stressed by the prior task's 282-call cron-toggle on large workflows). I restarted n8n to unblock — **which re-darkened the 141 schedule crons (T19) I'd restored last task.** Re-registering them (see report).
- HeyGen `video_url` is a temporary signed CDN URL (Expires) — plays now; download + re-host for permanence (future).
