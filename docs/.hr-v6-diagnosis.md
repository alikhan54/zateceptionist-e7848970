# HR V6 — Post-Ship Diagnosis (investigation-first)

**Branch:** `feature/hr-v6-postship-fixes` (off main `4d8031b`). **Method:** 5 parallel read-only agents across `src/`, prod DB (RLS-bypassed), n8n API. **Date:** 2026-05-30.

## Headline
**Most reported "bugs" are pure frontend wiring gaps over backends that already exist, are active, and work.** A few items are genuinely large missing-feature builds (outreach automation, AI-interview-by-VAPI end-to-end, training learning-package schema). Honest split below.

---

## TIER A — clear frontend bugs (backend ready; land these first)

### 1+2. Employee Edit dead / no Delete — `src/pages/hr/Employees.tsx`
- **Edit** (`:344`): `<DropdownMenuItem><Edit/>Edit</DropdownMenuItem>` has **no `onClick`**. ("Send Email" `:346` dead too.)
- **No Terminate** item anywhere; list view (`:403-405`) has no 3-dot menu; **no admin gating** used in any HR page.
- **Backend is ALL ready:** hook mutations `updateEmployee` (`useHR.ts:249-259` → `/hr/employee/update`) and `terminateEmployee` (`:261-271` → `/hr/employee/terminate`) exist + wired but **never invoked**. n8n `U9Yb2JSa46NlOKXC` HR7/HR8 active, write `hr_employees` + audit to `hr_audit_logs`. Soft-delete cols **exist**: `employment_status`, `termination_reason`, `termination_date` (NOTE: not `terminated_at`/`terminated_by`). `useIsHRAdmin()` exists (`useHR.ts:1387`).
- **Fix:** wire Edit → edit dialog calling `updateEmployee.mutate({id,...})`; add admin-gated Terminate → confirm dialog (reason) calling `terminateEmployee.mutate({id,reason,effective_date})`. **No backend/schema/hook work. EFFORT: quick-medium.**

### 9. Performance 360° "Start/Request Feedback" dead — `src/pages/hr/Performance.tsx:222,228`
- Both buttons have **no `onClick`, no dialog, no mutation**. No `hr_feedback`/`hr_360_feedback` table exists.
- **BUT** `hr_performance_reviews.review_type` CHECK already allows `'peer'|'360'` (the AGENTS.md "self|manager only" note is stale). So store 360° as a review row.
- **Fix:** add `isFeedbackOpen` + dialog (pick colleague + text) + `createFeedback` mutation inserting `hr_performance_reviews` with `review_type:'360'` (reuse `createReview`'s cycle-resolve at `useHR.ts:548-572`). **EFFORT: medium.**

### 10. Other Performance tabs (real vs stub)
- **Goals & OKRs — PARTIAL + quick bug:** real `hr_goals` fetch, but UI reads `goal.target_date` while the column is **`due_date`** → date badge never shows AND "Add Goal" target date is silently dropped (`createGoal` inserts non-existent `target_date`). **Fix: map `due_date`↔`target_date`. EFFORT: quick (3 lines).**
- **My Reviews — STUB:** `Performance.tsx:162-173` hard-coded empty state, ignores `data.reviews`. (zate has 9 real rows.) **Fix: filter `data.reviews` to current employee. EFFORT: medium.**
- **Team Reviews — PARTIAL:** real fetch but renders non-existent `review.employee_name`/`review.period` → blank names. **Fix: join `hr_employees`(name) + `hr_performance_cycles`(period). EFFORT: medium.**
- AI-Generate Review button IS wired (`A0M2juuizluBwASl`) but backend currently errors 400 (OpenAI-credits bug #97/#27) — document.

### 8. Candidate pipeline drill-down — `src/pages/hr/Recruitment.tsx`
- **Not dead** — detail opens via per-row dropdown "View Profile" (`:1491` → dialog `:2171`). Problem is **discoverability** (no whole-row click, no "Schedule AI Interview"/"Add phone" affordance from the row). **Fix: add row `onClick`. EFFORT: quick.**

### 11+12. Training AI course/video — `src/pages/hr/Training.tsx`
- AI pipeline (Claude gen `HTuKFLf8uiDnzPJA` + HeyGen `4u2H6AwbDnYcGQW5`) is **built, active, premium-enabled, proven** — but **0 UI call sites**. Webhooks `HR_TRAINING_GENERATE`/`HR_TRAINING_AVATAR_VIDEO` defined (`webhooks.ts:63-64`), never called.
- "Create Program" = plain CRUD insert (no AI). "AI Training Plan" = decoy (opens chat). **Only 1 video exists** (Patient Confidentiality, cosmique) — from a manual curl during the build session, stored in `hr_training_programs.provider` JSON (`avatar_video_url`). Player+quiz UI already render the full package.
- **Fix: add "Generate AI Course" dialog + `generateCourse` mutation → POST `/hr/training/generate`; + `generateAvatarVideo` mutation → POST `/hr/training/generate-avatar-video`. Backend done. EFFORT: quick-medium** (HeyGen poll ~4min → spinner or async).

### 13. Onboarding "Welcome to Your Business Hub" modal — `src/components/global/OnboardingFlow.tsx`
- Mounted in **`Layout.tsx:134` (FORBIDDEN file)** but the **fix lives in `OnboardingFlow.tsx` only**. It does **NOT** re-open on every navigation (Layout doesn't remount; Outlet swaps). It opens once-per-browser from `localStorage 'onboarding-completed'`, BUT: re-opens on every hard reload until Skip/Get-Started; **X/Esc/outside-click do NOT persist**; per-browser only (no DB/per-user). The warrior-QA saw it every test because Playwright uses a fresh context.
- **Fix (no forbidden-file edit): in `OnboardingFlow.tsx`, make `onOpenChange` persist on ALL close paths, and persist dismissal to `tenant_config.features.tutorial_dismissed` (additive, matches CLAUDE.md §6 convention). EFFORT: quick-medium.**

---

## TIER B/C — large builds & need-user-action (SCOPE, don't build blind)

### Recruitment architecture map (the where-is-it-logged answers) — issues 3-7
- **Candidate pool** → `hr_candidates`; **candidate↔job + all AI scores + stage** → `hr_job_applications`; **sourcing run audit** → `hr_sourcing_runs`; **every auto decision** → `hr_auto_decisions`.
- **AI interview log** → `hr_ai_interviews`: status=`status`, score=`ai_score`(int), recommendation=`recommendation`, duration=`actual_duration_seconds`. **Transcript** → `hr_ai_interviews.transcript`(jsonb). **Audio/recording** → `hr_ai_interviews.recording_url` = **VAPI's own hosted URL** (there is **NO Supabase storage bucket** for interview audio). Human interviews → `hr_interview_schedules` (empty).
- **Candidate approach/outreach** → intended `hr_recruitment_outreach` (+ mirror cols on `hr_job_applications.outreach_*`) — **but 0 rows all-time; NO workflow writes it.**
- **Workflow chain:** sourcing-v2 → `hr_candidates`+`hr_job_applications` → Auto-Pipeline `GoLKFQ3raVFyDg40` (15-min cron) advances by stage, fires AI Screening `VDDy59DDJsihAUAX` (Claude) → on "Schedule AI Interview" Q-Gen `MatQ3J4HYAgKiJ6A` inserts `hr_ai_interviews(status=scheduled)` → "Call Now" → Interview Call `bcaK7Lxd0HgtVfqW` (VAPI) → Receiver `0VEBSpO63nEiR1xh` writes transcript+recording+score.

### 3. Stuck "Scheduled" interview — ROOT CAUSE (triple)
The row (`54b441e5…`, Dr. Khalid Hamoudi, cosmique) is frozen because:
1. **Nothing dialed it** — Interview Call `bcaK7Lxd0HgtVfqW` has **0 executions ever**. Q-Gen creates `scheduled` but does NOT auto-dial; auto-pipeline has no dial rule; "Call Now" is a buried manual button nobody pressed.
2. Even if dialed → **candidate has no phone** (`phone=null`; only LinkedIn) → would PATCH `failed`.
3. Even with a phone → **cosmique has no VAPI config** (`tenant_config.vapi_api_key/assistant_id/phone_number_id` all NULL) → would PATCH `failed`.
→ `hr_ai_interviews` has exactly **1 row in the whole DB** and it's this stuck one. The AI-interview feature has **never completed end-to-end for any tenant.**
- **Action:** (a) Q-Gen should auto-fire start-call when phone+VAPI present, else insert `status='awaiting_phone'/'awaiting_vapi'` (not `scheduled`) so UI shows why [FIX, medium]; (b) populate cosmique VAPI creds [USER-ACTION]; (c) UI should disable AI-interview scheduling when no phone/VAPI [FIX, medium].

### 6. "Sourced 5, 0 actioned" — ROOT CAUSE
Screening **did** run (Claude scored all 5) and auto-pipeline **did** move them (1 advance, 3 hold, 1 reject). Real gaps: **(a)** no outreach automation exists at all (`hr_recruitment_outreach` dead) — **LARGE new build**; **(b)** apify enrichment returns **no email/phone** for 100% of candidates (T5 shallow-enrichment) — medium; **(c)** 3 stuck in grey-zone "hold" invisibly + **idempotency bug**: auto-pipeline re-writes a `hold` decision every 15 min → **723 dup rows for 5 candidates** — medium fix; **(d)** `total_candidates_matched`/`contacted` counters never populated → Sourcing tab shows misleading "Found 5 / Matched 0 / Contacted 0".

### 4/7. Sourcing status honesty + funnel clarity (Tier B candidates)
- "AI Sourcing: completed — N candidates" (`Recruitment.tsx:2020-2023`) is **dishonest** (reflects only "found", never screened/contacted/advanced). **Fix: show stage outcomes (advanced/held/rejected) instead of unpopulated matched/contacted. EFFORT: medium.**
- Two separate interview tabs ("Interviews"=`hr_interview_schedules` empty; "AI Interviews"=`hr_ai_interviews`) is a UX trap — clarify.

---

## PRIORITIZED FIX PLAN (this session)
**Land (Tier A, backend-ready, clear):** 1) Employee Edit+Terminate · 2) Training AI-course+video wiring · 3) Performance 360 feedback buttons · 4) Goals `due_date` quick bug · 5) Candidate row-click · 6) Onboarding modal persistence (OnboardingFlow.tsx only).
**Tier B (if budget):** sourcing status honesty + funnel clarity; My/Team Reviews tab fixes.
**Document as follow-up builds (large / user-action):** outreach automation (LARGE), AI-interview end-to-end (auto-dial + phone enrichment + per-tenant VAPI creds), training schema promotion + 5 types + program_id FK, hold-idempotency n8n fix, AI-review backend 400 (bug #97).
