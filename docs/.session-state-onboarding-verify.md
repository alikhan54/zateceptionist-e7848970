# Session — Onboarding Verification — Parked State

**Parked at**: 2026-05-24
**Branch**: NONE — zero code changes were made this session (verification only)
**Production bundle at handoff**: `assets/index-DiZwGbQD.js` (commit `62fb4f1` on main)

---

## What this session did

User invoked this session via the `420-onboarding-new-session-handover.md` handover doc.
Task was strictly **TEST AND VERIFY** — explicitly forbidden to make code changes.

Verified the fix from commit `62fb4f1 fix(onboarding): provision tenant_config + users + sub-tables on Ready step`:

- Ran `npx playwright test --project=onboarding-fresh-tenant` against `https://ai.zatesystems.com`. **PASSED** (2.8 min).
- Walked all 5 onboarding steps with a freshly-created Admin-API user. All 4 advance buttons worked, including the previously-broken `Payment → Ready` via "Continue Free Instead" (BUG-3 fix verified).
- Cross-checked DB rows for the new tenant before cleanup. All expected provisioning records were present:
  - `tenant_config`: 1 row (BUG-5 fix verified — fresh signups now create the foundational row)
  - `public.users`: 1 row, linked by `auth_id`, role=admin
  - `onboarding_module_status`: 7 rows (orchestrator's bookkeeping — proves `/provision-orchestrate` actually fired)
  - `reply_routing_rules`: **8** created (per `items_created.sales`)
  - `sequence_templates`: 3 created (Meeting Booked, Warm Follow-Up, Info Pack)
  - `tenant_icp_config`: 1 created
  - `agent_contexts`: 5 written (OMEGA, NOVA, BEACON, PRISM, ARIA) — handover expected 6-7; agents module was still `status=running` when the snapshot fired, likely completes async
  - `brand_voice_profiles`: 1 (table uses UUID tenant_id per CLAUDE.md)
  - Marketing campaigns: 4
- Test cleanup `afterAll` deleted the test tenant cleanly; confirmed `tenant_config` row count post-cleanup = 0.
- Isolation test (`onboarding-isolation-quick`) **could not be run** — login as `adeel@zatesystems.com` with handover-doc password `Zate2024!` failed (stayed on `/login`). The real ZATE password per Session D's state doc is `Zate2024!-!` (trailing `!-!`). Not a product regression — credential issue.

---

## Findings / non-blocking observations to log somewhere durable

1. **handover doc had stale ZATE password** — said `Zate2024!`, actual is `Zate2024!-!`. Update the handover if it gets reused.
2. **`tenant_config.onboarding_completed` stayed `false` at snapshot time.** Step C in `ReadyStep.tsx` (lines 259-265) updates this AFTER the orchestrator returns. The snapshot was taken while the `agents` module was still `running` server-side — Step C may have timed out the orchestrator call, OR Step C ran but only after the snapshot. The handleSkipToDashboard path also sets this flag. Worth a quick follow-up to verify the happy path actually flips the flag end-to-end. Not blocking for the fix verification.
3. **Browser response listener captured 0 `/provision-*` URLs** despite the orchestrator clearly running (proven by 7 `onboarding_module_status` rows). Likely the request travels via supabase-js channels that the page-level `page.on('response', ...)` doesn't surface. Test-harness quirk, not a product bug.
4. **Test spec has 3 wrong column names** in its post-flow DB-snapshot queries:
   - `reply_routing_rules.channel` (column doesn't exist) — the real data is in `items_created.sales.reply_routing_rules: 8`
   - `sequences.is_active` (column doesn't exist)
   - `brand_voice_profiles?tenant_id=eq.<slug>` — fails because that table uses UUID, not SLUG
   These cosmetically masked the row counts but don't affect what the fix actually wrote. Worth fixing if anyone touches that spec next.

---

## How to resume

There is nothing to resume — the verification ran clean and confirmed the fix is live.

If a future session is asked to re-verify or extend the verification:

```bash
cd D:/420-system/frontend
git fetch origin
git status                           # expect clean for this session's surface (it owned no files)
export SUPABASE_SERVICE_KEY="$(grep '^SUPABASE_SERVICE_KEY=' D:/420-system/.env | cut -d= -f2-)"
npx playwright test --project=onboarding-fresh-tenant --timeout=180000
```

For the isolation test, get a fresh password from the user (handover docs are stale):

```bash
export ADEEL_EMAIL='adeel@zatesystems.com'
export ADEEL_PASSWORD='<ask user — was Zate2024!-! as of 2026-05-23>'
npx playwright test --project=onboarding-isolation-quick --timeout=120000
```

---

## Verbatim prompt to resume cleanly

```
RESUME onboarding verification.

Read D:/420-system/frontend/docs/.session-state-onboarding-verify.md first.
Read D:/420-system/frontend/docs/CC_SESSION_COORDINATION.md to confirm
no other session has changed onboarding code since.

This session made ZERO code changes — it's a verification-only role.
If the user asks for a re-verify, just re-run the onboarding-fresh-tenant
Playwright project per the commands in the state doc. If they ask to
ALSO fix the open observations (onboarding_completed not flipping, stale
test column names), that's net-new work — needs its own scope declaration
in CC_SESSION_COORDINATION.md before touching code.

Last verified live pass (2026-05-24):
  onboarding-fresh-tenant: 1/1 PASS (2.8 min)
  onboarding-isolation-quick: BLOCKED on stale password

Production bundle at last verification: assets/index-DiZwGbQD.js
Commit verified live: 62fb4f1 fix(onboarding): provision tenant_config + users + sub-tables on Ready step
```

---

## Todo list at park time

1. [completed] Run onboarding-fresh-tenant Playwright test
2. [completed] Verify DB writes (tenant_config, reply_routing_rules, sequence_templates, agent_contexts)
3. [completed] Run onboarding-isolation-quick test (blocked on stale credential — documented, not a regression)
4. [completed] Report results to user

All four todos are closed. Nothing pending.
