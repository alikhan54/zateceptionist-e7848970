# Phase 12 — Pre-flight state save

**Date:** 2026-05-23

## Gates discovered

### ⚠ DDL gate (HARD BLOCKER for 12.A + 12.B)
- PostgREST cannot execute `CREATE TABLE` / `CREATE POLICY` / `ALTER TABLE`
- Auto-mode classifier blocked direct psycopg2 connection ("Production Reads rule" — even though my use case was a one-time schema add the user pre-approved)
- No `supabase` CLI installed locally
- No `supabase/migrations/` directory present
- **Resolution:** Phase 12.A schema written to `docs/PHASE12A_SCHEMA.sql` (idempotent, RLS-enabled). **User must paste this entire file into Supabase Studio → SQL Editor → Run.** Then 12.B can proceed.

### Deploy state
- Bundle: `index-ahawwjOO.js` (advanced past my Phase 11.5 `index-Bt8cbrWU.js` baseline)
- Recent commits on `main`: `23987d7 hr-recruitment`, `5535bef hr-e2e`, `d36583a phase11.5` — both my Phase 11.5 work AND additional non-cosmique HR work are pushed/deploying.

### Sacred file integrity
- `git status` on `NavigationSidebar.tsx`, `usePulseData.ts`, `sectionsRegistry.ts`, `formatCurrency.ts` — clean
- Working tree clean before Phase 12 starts

## Multi-tenant baseline snapshot (P.3)

Saved to `docs/MULTI_TENANT_BASELINE_PHASE12.json`. Cosmique starting state:
- clinic_treatments: 14, clinic_products: 3, clinic_patients: 3
- clinic_consultations: 1, appointments: 0
- sales_leads: 4, sequences: 3
- marketing_campaigns: 2, competitor_tracking: 3, blog_posts: 1
- cosmique-df4dd00d: all 0 (mandate-protected, must stay zero)

## Discovery during baseline
- `competitor_tracking` is **SLUG-based** for cosmique (3 rows via `tenant_id=cosmique`), NOT UUID as CLAUDE.md suggested. This explains why Phase 11.5 B.2 spec (`tenant_id=eq.<uuid>`) returned 0 rows. **Phase 12.E item:** patch the spec.

## REVISED PHASE 12 PLAN (DDL-aware)

| Sub-phase | Status | Action |
|---|---|---|
| P pre-flight | ✅ done | baseline + deploy + integrity verified |
| **12.A schema** | 🟡 **SQL READY, USER PASTE REQUIRED** | `docs/PHASE12A_SCHEMA.sql` — paste into Supabase Studio |
| 12.B UI on new schemas | ⏸️ DEFERRED | gated on 12.A apply |
| 12.C bulk+filter cross-apply | ▶ PROCEED | trim to 3 lists (patients, treatments, appointments) |
| 12.D industry pulse tabs | ▶ PROCEED | trim to 4 (Restaurant, RealEstate, BankingCollections, Tech) |
| 12.E walk backlog | ▶ PROCEED | 3-5 small fixes including competitor_tracking spec patch |
| 12.F master e2e | 🟡 DEFER | 25-min suite too expensive in budget |
| 12.G multi-tenant verification | ▶ PROCEED | existing tables only |
| 12.H final report | ▶ PROCEED | per-phase scoreboard + Phase 13 backlog |

## Token spend
~250 of 5000 used in pre-flight. Healthy buffer.
