# HR V3 — Session State (PARKED 2026-05-25 21:21)

## Status
PARKED — branch `parked/hr-v3-20260525-212103` (pushed to origin)

## Last commit on origin/main
- `71ab404` chore: trigger Lovable rebuild for 6d477be (real file upload)
- `6d477be` feat(hr-documents): real file upload (PDF/DOCX/TXT) + auto-extract + agent sync

## Git state at park
- HEAD == origin/main (no local-only commits)
- Park branch IS `origin/main` HEAD — no extra commits added
- Untracked / uncommitted files in working tree belong to OTHER sessions
  (phase7-results, NavigationSidebar.tsx.backup, marketing backups, etc.)
  — NOT touched.

## Open pending work (resume order)

### 1. Lovable deploy lag — CRITICAL, WAIT — DO NOT RE-PUSH
- Commit `71ab404` on origin/main since ~2026-05-25 20:38
- Live `Documents-DXhljmfZ.js` chunk still has the OLD "Coming Soon" placeholder
  (grep against the live JS confirmed "Upload File" / "Paste Text" / "file-input"
  testid all absent in the live chunk; "coming soon" present)
- File upload UI is on `origin/main` but not yet on `ai.zatesystems.com`
- Past sessions have seen Lovable lag 30-60+ min
- Resume command:
  ```bash
  until [ "$(curl -sL 'https://ai.zatesystems.com/' | grep -o 'Documents-[A-Za-z0-9_-]*\.js' | head -1)" != 'Documents-DXhljmfZ.js' ]; do sleep 30; done
  ```
- Then re-run D5:
  ```bash
  cd D:/420-system/frontend && ZATE_PASSWORD=$ZATE_PASSWORD \
    npx playwright test --project=hr-data-integrity --grep "D5 " --timeout=240000 --reporter=list
  ```

### 2. Sourcing pipeline Task Runner timeout (Open Bug #96)
- Pipeline reaches Phase 2 then `Task request timed out after 60 seconds`
- This session bumped `N8N_RUNNERS_TASK_TIMEOUT=3600` etc. but Phase 3/4 still don't complete
- Resume: split sourcing into smaller queue-chained workflows (same pattern as
  the onboarding v2 bypass)
- Or: switch sourcing's heavy nodes to external runner mode

### 3. Coordination doc registration of HR V3 workflows — DONE this park (Step 5)
- `31qSIf2I6VAF2loU` (Policy Sync v1.0) registered
- `i39PJEW8Z7IkFkUY` (Onboarding v2.0) registered
- `Tu7QL8CZdiyQCYGG` (Leave Request v2.0) registered

### 4. Contract compliance monitoring (NEW FEATURE — design)
- User requested in earlier prompt: when tenant uploads contract,
  daily cron checks employee data against contract terms + alerts on violations
- Foundation EXISTS: extracted contract rules already land in
  `hr_documents.extracted_rules.policy_rules` JSONB
- Resume after Lovable deploy lands (don't stack on top of unverified UI)

## Work completed this session

### 5 bugs fixed
- **BUG 1**: Employee data overwritten (`email` / `company_email` /
  `date_of_joining` / `emergency_contact_*` / `department_name`) — rewrote OB.2
  Process node + 5-line Employees.tsx fix. INPUT == DB verified 11/11 fields.
- **BUG 2**: "Job not persisting" — investigation showed it WAS persisting;
  cache-stale UI flake. No backend change needed. D2 strict spec confirms.
- **BUG 3**: Documents upload PGRST204 ("'name' column not found") — frontend
  now sends `document_name`/`title`/`document_type`.
- "File upload coming soon" placeholder removed; real file upload shipped.
- 1 stuck Cosmique sourcing run cleared; GP Aesthetics job reset to idle.

### Features shipped
- Real file upload — PDF (pdfjs-dist) / DOCX (mammoth) / TXT — landing in
  Supabase Storage bucket `hr-documents` (private, 10 MB max, mime allow-list).
- Policy sync — Documents auto-extract rules via Gemini and update every
  active AI agent's `knowledge_base.policies` + `system_prompt`.
- "Policies Enforced" card on AIAgentProfile.tsx (blue Shield, rule count).

### Tests added (all on origin/main, in `frontend/tests/`)
- `hr-data-integrity.spec.ts` — D1 (employee fields) / D2 (job persistence) /
  D3 (document upload) / D4 (policy sync) / D5 (real file upload, setInputFiles)
- `hr-hiring-complete.spec.ts` — 18-test world-class hiring proof (committed
  earlier this session)
- `hr-sourcing-pipeline.spec.ts` — 4-test sourcing UI verification

### Storage / DB changes (applied directly to live Supabase)
- `INSERT INTO storage.buckets ('hr-documents', private, 10485760, mime list)`
- 4× `CREATE POLICY` on `storage.objects` for `bucket_id='hr-documents'`
- `ALTER TABLE hr_documents ADD COLUMN IF NOT EXISTS document_content TEXT`
- `ALTER TABLE hr_documents ADD COLUMN IF NOT EXISTS extracted_rules JSONB`
- `ALTER TABLE hr_documents ADD COLUMN IF NOT EXISTS sync_status TEXT`
- `ALTER TABLE hr_documents ADD COLUMN IF NOT EXISTS synced_at TIMESTAMPTZ`
- `ALTER TABLE hr_documents ADD COLUMN IF NOT EXISTS sync_error TEXT`
- `ALTER TABLE hr_documents ADD COLUMN IF NOT EXISTS uploaded_by UUID`

## Decision points pending
None.

## Files owned by HR V3 (for coord doc)

Frontend:
- `src/pages/hr/*` (all HR pages)
- `src/hooks/useHR.ts`
- `src/hooks/useAIAgents.ts`
- `src/hooks/useRecruitment.ts`
- `src/components/hr/*`
- `src/lib/api/webhooks.ts` — **only** the HR-namespaced constants
  (`HR_DOCUMENT_SYNC`, `EMPLOYEE_ONBOARDING`, etc.). Shared file — other
  sessions own other lines.
- `frontend/tests/hr-*.spec.ts`
- `frontend/tests/HR_*_REPORT.md`

n8n workflows:
- `31qSIf2I6VAF2loU` (HR Policy Sync v1.0) — created 2026-05-25
- `i39PJEW8Z7IkFkUY` (HR Onboarding v2.0) — patched 2026-05-25 (OB.2 jsCode)
- `Tu7QL8CZdiyQCYGG` (HR Leave Request v2.0)
- `HIxXgBxEVAJI1KuL` (AI Agent Metrics)
- `FkEfBn8od7xrJWEX` (AI Agent Learning)
- `mlsC24hFDv6O7GyG` (AI Agent Activator)
- `azItflaNjJxpeYXu` (AI Agent Creator)

n8n workflows TOUCHED (sacred, owned by no session — patched with user approval):
- `tHIN8s5hurqzRU7g` (HR Part 2 sourcing) — 6 nodes patched in earlier session;
  details in `frontend/tests/HR_SOURCING_PIPELINE_FIX_REPORT.md`

## Audit artifacts
- `D:/420-system/.tmp_intent_audit/06_hr_new_workflows.md` — metadata for the 3
  HR V3 workflows captured at park time
- `D:/420-system/.tmp_intent_audit/park_branch.txt` — park branch name

## Verbatim resume prompt

```
RESUME HR V3 from parked/hr-v3-20260525-212103.

1. Read frontend/docs/.session-state-hr-v3.md first (this file).
2. Read frontend/docs/CC_SESSION_COORDINATION.md to confirm no other session
   has touched HR V3 scope while I was parked.
3. Verify Lovable deploy:
     curl -sL https://ai.zatesystems.com/ | grep -o 'Documents-[A-Za-z0-9_-]*\.js' | head -1
   If still 'Documents-DXhljmfZ.js' — wait OR ask user to nudge the Lovable
   pipeline manually.
4. Once chunk hash changes:
     ZATE_PASSWORD='Zate2024!-!' npx playwright test \
       --project=hr-data-integrity --grep "D5 " --timeout=240000 --reporter=list
   Should pass — code on main is byte-identical to what passed locally.
5. Next features (in priority order):
   a. Contract compliance monitoring (daily cron over hr_documents.extracted_rules)
   b. Sourcing Task Runner timeout fix (split sourcing into queue-chained workflows)
   c. PDF OCR for scanned documents (current parser is text-PDF only)
```
